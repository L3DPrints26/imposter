# Pocket Imposter — Rules

The full rules for the Pocket Imposter handheld party game, linked from the QR
code on the device's help screen.

One self-contained HTML file: no scripts, no tracking, no external requests, so
it loads instantly on a bad playground signal. Marked `noindex` so it stays
unlisted — findable by anyone holding the device, not by search engines.
